name: Docs encoding sanity

on:
  pull_request:
  push:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Fail on mojibake
        run: |
          set -euo pipefail
          # Check markdown files only (avoids self-reference in workflow)
          if grep -RIn --include="*.md" -E "â€[œ™"]|Ã[‰.]" . 2>/dev/null; then
            echo "Found mojibake in documentation. Fix encoding before merge."
            exit 1
          fi