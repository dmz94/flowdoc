# Test Fixtures

This directory is intentionally empty. Fixture locations:

- **tests/pipeline-audit/test-pages/** -- Evaluation corpus HTML files
- **tests/pipeline-audit/expected-results/** -- Baseline JSON records
- **tests/unit/test-data/** -- Small HTML fixtures used by unit tests
